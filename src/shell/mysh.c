#include <stdio.h>

#define MAX_LENGTH 500

int main() {
    while (1==1) {
        char input[MAX_LENGTH];
        printf("Enter cmd\n");
        scanf("%s", input);
        printf("%s\n", input);
    }
    return 1;
}
